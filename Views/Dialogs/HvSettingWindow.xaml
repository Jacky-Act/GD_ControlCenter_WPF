<Window x:Class="GD_ControlCenter_WPF.Views.Dialogs.HvSettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="高压电源参数设置" 
        Height="350" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="3*"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,0">
            <TextBlock Text="参数配置 - 高压电源" Style="{StaticResource MaterialDesignHeadline6TextBlock}" />
            <Separator Margin="0,8,0,0" Style="{StaticResource MaterialDesignLightSeparator}" />
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Center" Grid.RowSpan="2">
            <TextBlock Text="电压设置 (V)" Style="{StaticResource MaterialDesignBody2TextBlock}" Opacity="0.6" />
            <Grid Margin="0,8,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <Slider Minimum="0" Maximum="1500" 
                        Value="{Binding TargetVoltage, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource MaterialDesign3.MaterialDesignDiscreteSlider}"
                        VerticalAlignment="Center"
                        TickFrequency="1" />
                <TextBox Grid.Column="1" Text="{Binding TargetVoltage, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="电压"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                         Height="45" TextAlignment="Center" Margin="12,0,0,0" />
            </Grid>

            <TextBlock Text="电流设置 (mA)" Style="{StaticResource MaterialDesignBody2TextBlock}" Opacity="0.6" />
            <Grid Margin="0,8,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <Slider Minimum="0" Maximum="100" 
                        Value="{Binding TargetCurrent, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource MaterialDesign3.MaterialDesignDiscreteSlider}"
                        VerticalAlignment="Center"
                        TickFrequency="1" />
                <TextBox Grid.Column="1" Text="{Binding TargetCurrent, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="电流"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                         Height="45" TextAlignment="Center" Margin="12,0,0,0" />
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="取消" IsCancel="True" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,10,0" Width="100" />
            <Button Content="应用" Command="{Binding ApplyCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Width="100" />
        </StackPanel>
    </Grid>
</Window>