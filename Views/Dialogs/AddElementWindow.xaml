<Window x:Class="GD_ControlCenter_WPF.Views.Dialogs.AddElementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="元素周期表 - 选择分析元素" Height="500" Width="850" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="ToolWindow"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 核心：元素周期表网格 -->
        <ItemsControl Grid.Row="0" ItemsSource="{Binding PeriodicElements}">
            <!-- 定义 18 列 x 7 行的大网格 -->
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <!-- 18 个等宽列 -->
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <!-- 7 个等高行 (目前只排主族和过渡，暂省镧系锕系，若需可加至9行) -->
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <!-- 将每个元素模型绑定到 Grid 的对应 Row 和 Column -->
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Grid.Row" Value="{Binding Row}" />
                    <Setter Property="Grid.Column" Value="{Binding Column}" />
                    <Setter Property="Margin" Value="2"/>
                </Style>
            </ItemsControl.ItemContainerStyle>

            <!-- 每个元素的长相 (漂亮的小方块) -->
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <!-- 点击按钮，触发 ViewModel 中的 SelectElement 命令，并把 Symbol (如 Pb) 传进去 -->
                    <Button Command="{Binding DataContext.SelectElementCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding Symbol}"
                            Background="{Binding HexColor}"
                            BorderThickness="0"
                            materialDesign:ElevationAssist.Elevation="Dp1"
                            ToolTip="{Binding AtomicNumber, StringFormat='原子序数: {0}'}"
                            Padding="0">
                        <StackPanel VerticalAlignment="Center">
                            <!-- 左上角的小号原子序数 -->
                            <TextBlock Text="{Binding AtomicNumber}" FontSize="10" Foreground="#555" 
                                       HorizontalAlignment="Left" Margin="4,2,0,0"/>
                            <!-- 居中的大号元素符号 -->
                            <TextBlock Text="{Binding Symbol}" FontSize="20" FontWeight="Bold" 
                                       Foreground="Black" HorizontalAlignment="Center" Margin="0,-4,0,4"/>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 底部关闭按钮 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="取消并关闭" Style="{StaticResource MaterialDesignOutlinedButton}" 
                    Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>