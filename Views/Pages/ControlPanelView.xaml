<UserControl x:Class="GD_ControlCenter_WPF.Views.Pages.ControlPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:components="clr-namespace:GD_ControlCenter_WPF.Views.Components" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <Grid.RowDefinitions>
            <RowDefinition Height="4*" />
            <RowDefinition Height="6*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="6*" />
                    <ColumnDefinition Width="4*" />
                </Grid.ColumnDefinitions>

                <components:DeviceControlCard Grid.Column="0" 
                                              DataContext="{Binding Devices[0]}" 
                                              Margin="5,5,0,2.5"/>
                <components:DeviceControlCard Grid.Column="1" 
                                              DataContext="{Binding Devices[1]}" 
                                              Margin="5,5,5,2.5"/>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.5*" />
                    <ColumnDefinition Width="1.5*" />
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="4*" />
                </Grid.ColumnDefinitions>

                <materialDesign:Card Grid.Column="0" Margin="5,5,0,5" UniformCornerRadius="8"
                                     materialDesign:ElevationAssist.Elevation="Dp2">
                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                    Height="Auto" Padding="0">
                        <TextBlock Text="点火" FontSize="24" FontWeight="Bold" Foreground="IndianRed" 
                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button>
                </materialDesign:Card>

                <materialDesign:Card Grid.Column="1" Margin="5,2.5,0,5" UniformCornerRadius="8"
                                     materialDesign:ElevationAssist.Elevation="Dp2">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="转向阀门" Margin="0,0,0,8" HorizontalAlignment="Center" FontSize="12" Opacity="0.6"/>
                        <!--<ToggleButton Style="{StaticResource MaterialDesignSwitchToggleButton}" />-->


                        <ToggleButton Style="{StaticResource MaterialDesignSwitchToggleButton}"
                            ToolTip="MaterialDesignSwitchToggleButton with Content and ToggleButtonAssist.OnContent">
                            <materialDesign:ToggleButtonAssist.SwitchTrackOnBackground>
                                <SolidColorBrush Color="#673ab7" />
                            </materialDesign:ToggleButtonAssist.SwitchTrackOnBackground>
                            <materialDesign:ToggleButtonAssist.SwitchTrackOffBackground>
                                <SolidColorBrush Color="#3a7e00" />
                            </materialDesign:ToggleButtonAssist.SwitchTrackOffBackground>

                            <materialDesign:PackIcon Kind="Pin" RenderTransformOrigin=".5,.5">
                                <materialDesign:PackIcon.RenderTransform>
                                    <RotateTransform Angle="45" />
                                </materialDesign:PackIcon.RenderTransform>
                            </materialDesign:PackIcon>

                            <materialDesign:ToggleButtonAssist.OnContent>
                                <materialDesign:PackIcon Kind="Pin" />
                            </materialDesign:ToggleButtonAssist.OnContent>
                        </ToggleButton>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Grid.Column="2" Margin="5,2.5,0,5" UniformCornerRadius="8"
                                     materialDesign:ElevationAssist.Elevation="Dp2">
                    <Button Command="{Binding Devices[3].OpenDetailCommand}"
                Style="{StaticResource MaterialDesignFlatButton}"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                Height="Auto" Padding="0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <materialDesign:PackIcon Kind="AxisArrow" VerticalAlignment="Center" Margin="0,0,8,0" 
                                        Foreground="{Binding Devices[3].ThemeColor}"/>
                            <TextBlock Text="三维平台控制" FontSize="24" FontWeight="Bold" 
                           Foreground="{Binding Devices[3].ThemeColor}"/>
                        </StackPanel>
                    </Button>
                </materialDesign:Card>

                <components:DeviceControlCard Grid.Column="3" 
                                  DataContext="{Binding Devices[2]}" 
                                  Margin="5,2.5,5,5"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>