<UserControl x:Class="GD_ControlCenter_WPF.Views.Pages.ElementConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 核心三列布局 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.5*" />
                <!-- 元素列 -->
                <ColumnDefinition Width="3*" />
                <!-- 波长列 -->
                <ColumnDefinition Width="6.5*" />
                <!-- 参数与已选配置列 -->
            </Grid.ColumnDefinitions>

            <!-- 1. 检测元素 -->
            <materialDesign:Card Grid.Column="0" Margin="5" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock Text=" 检 测 元 素 " Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Opacity="0.8"/>
                        <Separator Margin="0,4,0,4" Style="{StaticResource MaterialDesignLightSeparator}"/>
                    </StackPanel>
                    <ListBox Grid.Row="1" ItemsSource="{Binding Elements}" SelectedItem="{Binding SelectedElement, Mode=TwoWay}" Style="{StaticResource MaterialDesignListBox}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="4">
                                    <materialDesign:PackIcon Kind="ChartBubble" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </materialDesign:Card>

            <!-- 2. 目标波长 -->
            <materialDesign:Card Grid.Column="1" Margin="5" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock Text=" 目 标 波 长 " Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Opacity="0.8"/>
                        <Separator Margin="0,4,0,4" Style="{StaticResource MaterialDesignLightSeparator}"/>
                    </StackPanel>
                    <ListBox Grid.Row="1" ItemsSource="{Binding Wavelengths}" SelectedItem="{Binding SelectedWavelength, Mode=TwoWay}" Style="{StaticResource MaterialDesignListBox}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="4">
                                    <materialDesign:PackIcon Kind="SineWave" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryHueMidBrush}" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding Value, StringFormat={}{0} nm}" VerticalAlignment="Center" FontSize="15"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </materialDesign:Card>

            <!-- 3. 右侧大屏：参数同步 + 已选配置 -->
            <Grid Grid.Column="2" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 上方：仪表台参数同步卡片 -->
                <materialDesign:Card Grid.Row="0" Margin="0,0,0,10" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2" Background="{DynamicResource PrimaryHueLightBrush}" Foreground="{DynamicResource PrimaryHueLightForegroundBrush}">
                    <Grid Margin="15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="仪表台硬件参数实时同步：" FontWeight="Bold" Margin="0,0,0,10"/>

                        <UniformGrid Grid.Row="1" Columns="4">
                            <StackPanel>
                                <TextBlock Text="高压电流 (mA)" Opacity="0.8" FontSize="12"/>
                                <TextBlock Text="{Binding GlobalCurrent}" FontSize="20" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="蠕动泵速 (rpm)" Opacity="0.8" FontSize="12"/>
                                <TextBlock Text="{Binding GlobalPumpSpeed}" FontSize="20" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="采样次数" Opacity="0.8" FontSize="12"/>
                                <TextBlock Text="{Binding GlobalSampleCount}" FontSize="20" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="采样间隔 (s)" Opacity="0.8" FontSize="12"/>
                                <TextBlock Text="{Binding GlobalSampleInterval}" FontSize="20" FontWeight="Bold"/>
                            </StackPanel>
                        </UniformGrid>
                    </Grid>
                </materialDesign:Card>

                <!-- 下方：已选分析配置大表 -->
                <materialDesign:Card Grid.Row="1" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2">
                    <Grid Margin="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Text=" 已 选 分 析 配 置 " Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Opacity="0.8"/>
                            <Separator Margin="0,4,0,4" Style="{StaticResource MaterialDesignLightSeparator}"/>
                        </StackPanel>

                        <!-- 剔除了曲线列，只保留硬件参数 -->
                        <DataGrid Grid.Row="1" ItemsSource="{Binding SelectedConfigs}" SelectedItem="{Binding CurrentSelectedConfig, Mode=TwoWay}"
                                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                                  materialDesign:DataGridAssist.ColumnHeaderPadding="8" Margin="0,5,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="元素" Binding="{Binding ElementName}" Width="1*"/>
                                <DataGridTextColumn Header="波长 (nm)" Binding="{Binding Wavelength}" Width="1.5*"/>
                                <DataGridTextColumn Header="电流(mA)" Binding="{Binding CurrentConfigText}" Width="1*"/>
                                <DataGridTextColumn Header="泵速(rpm)" Binding="{Binding SpeedConfigText}" Width="1*"/>
                                <DataGridTextColumn Header="采样次数" Binding="{Binding SampleCountText}" Width="1*"/>
                                <DataGridTextColumn Header="间隔(s)" Binding="{Binding SampleIntervalText}" Width="1*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </materialDesign:Card>
            </Grid>
        </Grid>

        <!-- 底部控制按钮组 (去除了计算拟合等) -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,10">
            <Button Content="添加元素" Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding AddElementCommand}" Margin="0,0,20,0"/>
            <Button Content="添加波长" Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding AddWavelengthCommand}" Margin="0,0,50,0"/>

            <Button Content="加入分析配置" Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding SelectCurrentConfigCommand}" ToolTip="将上方选中的组合添加至已选列表" Margin="0,0,20,0"/>
            <Button Content="删除选中配置" Style="{StaticResource MaterialDesignOutlinedButton}" Command="{Binding RemoveConfigCommand}" Foreground="IndianRed" BorderBrush="IndianRed"/>
        </StackPanel>
    </Grid>
</UserControl>