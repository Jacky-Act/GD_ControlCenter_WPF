<UserControl x:Class="GD_ControlCenter_WPF.Views.Pages.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid Margin="16">
        <StackPanel>
            <materialDesign:Card Padding="24" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="通信设置" Style="{StaticResource MaterialDesignHeadline6TextBlock}" />
                    <Separator Margin="0,8,0,16" Style="{StaticResource MaterialDesignLightSeparator}" />

                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="200" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="串口号" VerticalAlignment="Center" Opacity="0.8" Margin="0,0,16,0" FontSize="14"/>

                        <ComboBox Grid.Column="1" 
                                  ItemsSource="{Binding AvailablePorts}" 
                                  SelectedItem="{Binding SelectedPort, Mode=TwoWay}"
                                  materialDesign:HintAssist.Hint="未检测到串口"
                                  materialDesign:TextFieldAssist.UnderlineBrush="{DynamicResource PrimaryHueMidBrush}"
                                  VerticalAlignment="Center" FontSize="14"/>

                        <Button Grid.Column="2" 
                                Style="{StaticResource MaterialDesignIconButton}" 
                                Command="{Binding RefreshPortsCommand}" 
                                ToolTip="刷新设备列表并尝试自动连接"
                                Margin="8,0,0,0">
                            <materialDesign:PackIcon Kind="Refresh" Width="20" Height="20" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                        </Button>

                        <Button Grid.Column="3" 
                                Content="连接" 
                                Command="{Binding ConnectCommand}" 
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                Margin="24,0,0,0" Width="100"/>

                        <TextBlock Grid.Column="4" 
                                   Text="{Binding StatusText}" 
                                   HorizontalAlignment="Right" 
                                   VerticalAlignment="Center" 
                                   Opacity="0.8" 
                                   FontSize="14" 
                                   FontWeight="Bold"/>
                    </Grid>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Padding="24" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2">
                <StackPanel>
                    <TextBlock Text="控制与安全配置" Style="{StaticResource MaterialDesignHeadline6TextBlock}" />
                    <Separator Margin="0,8,0,16" Style="{StaticResource MaterialDesignLightSeparator}" />

                    <Grid Margin="0,8,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="异常熄火自动点火" FontSize="16" />
                            <TextBlock Text="在系统运行期间，若检测到意外熄火，将自动触发重燃序列" FontSize="12" Opacity="0.6" Margin="0,4,0,0" />
                        </StackPanel>

                        <ToggleButton Grid.Column="1" 
                                      IsChecked="{Binding IsAutoReigniteEnabled}"
                                      Style="{StaticResource MaterialDesignSwitchToggleButton}" 
                                      ToolTip="开启或关闭熄火自动重燃" />
                    </Grid>

                    <Separator Margin="0,0,0,16" Style="{StaticResource MaterialDesignLightSeparator}" Opacity="0.5"/>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="光谱仪硬件连接使能" FontSize="16" />
                            <TextBlock Text="全局控制光谱仪的物理连接与数据交互权限" FontSize="12" Opacity="0.6" Margin="0,4,0,0" />
                        </StackPanel>

                        <ToggleButton Grid.Column="1" 
                                      IsChecked="{Binding IsSpectrometerEnabled}"
                                      Style="{StaticResource MaterialDesignSwitchToggleButton}" 
                                      ToolTip="启用或断开光谱仪硬件连接" />
                    </Grid>

                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </Grid>
</UserControl>