<UserControl x:Class="GD_ControlCenter_WPF.Views.Pages.SampleMeasurementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             xmlns:helpers="clr-namespace:GD_ControlCenter_WPF.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1366">

    <UserControl.Resources>
        <helpers:InverseBoolConverter x:Key="InverseBoolConverter" />
    </UserControl.Resources>

    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.5*" />
            <!-- 1. 序列 -->
            <ColumnDefinition Width="5*" />
            <!-- 2. 图表 -->
            <ColumnDefinition Width="2.5*" />
            <!-- 3. 控制 -->
        </Grid.ColumnDefinitions>

        <!-- 左侧：序列 -->
        <materialDesign:Card Grid.Column="0" Margin="5" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="待测任务序列" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" Opacity="0.6"/>
                <DataGrid Grid.Row="1" ItemsSource="{Binding MeasurementSequence}" SelectedItem="{Binding CurrentSample}"
                          AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="85">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="采集数据中...">
                                            <Setter Property="Foreground" Value="Orange"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="已完成">
                                            <Setter Property="Foreground" Value="Green"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="样品名称" Binding="{Binding SampleName}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <!-- 中间：双谱图切换 -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 上图：全谱图 -->
            <materialDesign:Card Grid.Row="0" Margin="2.5,5" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2">
                <Grid>
                    <TextBlock Text="实时全谱监控 (Full Spectrum)" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="15,10" Opacity="0.5" Style="{StaticResource MaterialDesignSubtitle2TextBlock}"/>
                    <ScottPlot:WpfPlot x:Name="SpecPlot" Margin="5,35,5,5"/>
                </Grid>
            </materialDesign:Card>

            <!-- 下图：元素局部图 -->
            <materialDesign:Card Grid.Row="1" Margin="2.5,5" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="15,10" Panel.ZIndex="1">
                        <TextBlock Text="当前查看元素：" VerticalAlignment="Center" Opacity="0.6"/>
                        <ComboBox ItemsSource="{Binding PickedElements}" SelectedItem="{Binding SelectedElement}" Width="130" Margin="10,0,0,0"/>
                    </StackPanel>
                    <TextBlock Text="元素局部特征峰" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="15,10" Opacity="0.5" Style="{StaticResource MaterialDesignSubtitle2TextBlock}"/>
                    <ScottPlot:WpfPlot x:Name="TrendPlot" Margin="5,45,5,5"/>
                    <!-- 后台逻辑仍使用TrendPlot这个Name -->
                </Grid>
            </materialDesign:Card>
        </Grid>

        <!-- 右侧：结果与动态按钮 -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <materialDesign:Card Margin="5" UniformCornerRadius="8" Background="{DynamicResource PrimaryHueMidBrush}" Foreground="White">
                <StackPanel Margin="15">
                    <TextBlock Text="{Binding CurrentSample.SampleName, FallbackValue='未选择'}" FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="{Binding CurrentSample.Type}" FontSize="14" Opacity="0.8"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Row="1" Margin="5" UniformCornerRadius="8">
                <DataGrid ItemsSource="{Binding CurrentSample.ElementConcentrations}" AutoGenerateColumns="False" IsReadOnly="True" Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="元素" Binding="{Binding ElementName}" Width="*"/>
                        <DataGridTextColumn Header="平均强度" Binding="{Binding MeasuredIntensity, StringFormat={}{0:F2}}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </materialDesign:Card>

            <materialDesign:Card Grid.Row="2" Margin="5" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp3">
                <Grid Margin="10">
                    <Button Height="70" FontSize="20" FontWeight="Bold">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="Content" Value="开始采集数据"/>
                                <Setter Property="Command" Value="{Binding StartCollectingCommand}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCollecting}" Value="True">
                                        <Setter Property="Content" Value="停止采集并保存"/>
                                        <Setter Property="Background" Value="IndianRed"/>
                                        <Setter Property="BorderBrush" Value="IndianRed"/>
                                        <Setter Property="Command" Value="{Binding StopAndSaveCommand}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>